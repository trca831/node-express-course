const num1 = 5;
const num2 = 10;

function addValues(){
    console.log(`the sum is: ${num1 + num2}`)
}

addValues() 
//function inside module that we invoke the code will run
//even though we did not assign it to any variable 